package com.aifriend.ai_friend

import io.flutter.embedding.android.FlutterActivity

class MainActivity : FlutterActivity() {
    // STT ใช้ speech_to_text package แล้ว — ไม่ต้อง native MethodChannel
}
